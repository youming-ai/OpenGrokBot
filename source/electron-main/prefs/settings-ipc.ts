interface SyncIpc { on(channel: string, listener: (event: { returnValue: unknown }) => void): void }
export function registerSettingsIpc(deps: { readonly ipcMain: SyncIpc; readonly settingsStore: { getEgressTunnelEnabled(): unknown; getWebauthnProxyEnabled(): unknown }; readonly themeController: { getState(): unknown }; readonly egressTunnelController: { getStatus(): unknown } }): void { deps.ipcMain.on("sand:theme-get-sync", (event) => { event.returnValue = deps.themeController.getState(); }); deps.ipcMain.on("sand:egress-tunnel-get-sync", (event) => { event.returnValue = deps.settingsStore.getEgressTunnelEnabled(); }); deps.ipcMain.on("sand:egress-tunnel-status-get-sync", (event) => { event.returnValue = deps.egressTunnelController.getStatus(); }); deps.ipcMain.on("sand:webauthn-proxy-get-sync", (event) => { event.returnValue = deps.settingsStore.getWebauthnProxyEnabled(); }); }
