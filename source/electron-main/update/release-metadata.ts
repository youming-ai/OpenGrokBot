import { isSandUpdateTrack, type SandUpdateTrack } from "../../shared/update-track.js";
import { computeUpdateDisabledReason } from "./update-gate.js";
export function createReleaseMetadata(deps: { readonly packageVersion?: unknown; readonly packageTrack?: unknown; readonly isLabBuild: boolean; readonly app: { getVersion(): string; readonly isPackaged: boolean }; readonly env?: NodeJS.ProcessEnv; readonly platform?: NodeJS.Platform }) { const env = deps.env ?? process.env; return { readAppReleaseMetadata: (): { version: string; buildDefaultTrack: SandUpdateTrack | null } => ({ version: typeof deps.packageVersion === "string" ? deps.packageVersion : deps.app.getVersion(), buildDefaultTrack: isSandUpdateTrack(deps.packageTrack) ? deps.packageTrack : null }), computeUpdateDisabledReasonLive: async () => computeUpdateDisabledReason({ envDisabled: env.SAND_DISABLE_UPDATES === "1", isLabBuild: deps.isLabBuild, hasDevFeedOverride: (env.SAND_UPDATE_FEED_BASE_URL?.length ?? 0) > 0, isPackaged: deps.app.isPackaged, platform: deps.platform ?? process.platform }) }; }
