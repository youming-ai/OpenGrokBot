export type UpdateDisabledReason = "disabled-by-env" | "lab-build" | "not-packaged" | "unsupported-platform";
export function computeUpdateDisabledReason(input: { readonly envDisabled: boolean; readonly isLabBuild: boolean; readonly hasDevFeedOverride: boolean; readonly isPackaged: boolean; readonly platform: NodeJS.Platform }): UpdateDisabledReason | null { if (input.envDisabled) return "disabled-by-env"; if (input.isLabBuild) return "lab-build"; if (input.hasDevFeedOverride) return null; if (!input.isPackaged) return "not-packaged"; return input.platform === "darwin" || input.platform === "win32" ? null : "unsupported-platform"; }
