export interface BoxVncSender { readonly isBoxVncPartition: boolean; readonly frameUrl?: string | null }
export function createBoxVncTrust(isTrustedBoxDesktopFrameUrl: (url: string) => boolean) { return { boxDesktopWebview: { kind: "require" as const, test: (sender: BoxVncSender) => sender.isBoxVncPartition && sender.frameUrl != null && sender.frameUrl.length > 0 && isTrustedBoxDesktopFrameUrl(sender.frameUrl), denial: "The box-VNC edge is only accessible from the trusted box-desktop webview." } }; }
export function createBoxVncHandlers(deps: { readonly readClipboardText: () => string; readonly writeClipboardText: (text: string) => void; readonly onUserPresence: (isPresent: boolean) => void }) { return { boxDesktopWebview: { readClipboard: () => deps.readClipboardText(), writeClipboard: ({ text }: { text?: unknown }) => { if (typeof text === "string" && text.length > 0) deps.writeClipboardText(text); }, reportUserPresence: ({ isPresent }: { isPresent?: unknown }) => { if (typeof isPresent === "boolean") deps.onUserPresence(isPresent); } } }; }
