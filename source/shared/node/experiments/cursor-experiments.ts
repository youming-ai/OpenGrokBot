import { LogEventCompressionMode, StatsigClient, type StatsigUser } from "@statsig/js-client";
import { createDeadlinePolicy, createPollingPolicy, DeadlineExceededError, realClock, type PollingPolicy } from "../../../internal/scheduling.js";
import { errorLogTag } from "../../errors.js";
import { getConfiguredBackendUrl } from "../cursor-token.js";
import { DYNAMIC_CONFIGS, EXPERIMENTS, FLAGS, type DynamicConfigName, type ExperimentName, type FeatureFlagName } from "./experiment-config.gen.js";
import { SandFeatureFlagOverrideStore, isFlagName } from "./feature-flag-overrides.js";
import { MutableGateProperty } from "./gate-property.js";
import { reportExperimentsDiagnostic } from "./experiments-diagnostics.js";
import { SAND_AUTOMATIONS_MODEL_CONFIG_NAME, SAND_DEFAULT_MODEL_CONFIG_NAME, SAND_MODEL_FILTER_CONFIG_NAME, resolveSandDefaultModelConfig, type ModelConfigRejection } from "./sand-model-config.js";
import { SAND_MODEL_EXPERIMENT_NAME, readSandModelExperimentEnvOverride, resolveSandModelExperimentState } from "./sand-model-experiment.js";
import { STATSIG_CLIENT_KEY, STATSIG_LOG_EVENT_PROXY_URL, extractStatsigUser, fetchStatsigBootstrap, loadCachedBootstrap, readStatsigBootstrapUserId, sandStatsigNetworkOverride, saveCachedBootstrap } from "./statsig-bootstrap.js";

export const BASE_POLL_INTERVAL_MS = 5 * 60_000; export const MIN_POLL_INTERVAL_MS = 30_000; export const POLL_JITTER_FRACTION = 0.3; export const BOOTSTRAP_TIMEOUT_MS = 30_000; export const GATE_READY_TIMEOUT_MS = 10_000;
const READ_OPTIONS = { disableExposureLog: true } as const;
export function computePollDelayMs(args: { baseIntervalMs: number; jitterRoll: number; retryAfterMs?: number }): number { const jittered = Math.floor(args.baseIntervalMs * (1 + args.jitterRoll * POLL_JITTER_FRACTION)); return args.retryAfterMs == null ? jittered : Math.max(args.retryAfterMs, jittered); }
export function envGateOverride(name: string, env: NodeJS.ProcessEnv = process.env): boolean | undefined { const raw = env.SAND_FEATURE_GATE_OVERRIDES; if (raw == null || raw.length === 0) return undefined; for (const pair of raw.split(",")) { const [key, value] = pair.split("=", 2).map((part) => part.trim()); if (key === name) return value === "1" || value === "true" || value === "on"; } return undefined; }
export function jitteredSandExperimentPollIntervalMs(isDevBuild: boolean, random = Math.random): number { const base = isDevBuild ? MIN_POLL_INTERVAL_MS : Math.max(BASE_POLL_INTERVAL_MS, MIN_POLL_INTERVAL_MS); return Math.floor(base * (1 + random() * POLL_JITTER_FRACTION)); }

type Trigger = "startup" | "poll" | "auth_change" | "manual";
type Snapshot = { isInitialized: boolean; featureGates: Record<string, boolean>; experiments: Record<string, Record<string, unknown>>; dynamicConfigs: Record<string, Record<string, unknown>>; sandModelExperiment?: unknown; sandModelFilterAllowedIds?: string[]; featureFlags?: unknown };
export class SandExperimentService {
  private client: StatsigClient | null = null; private isInitialized = false; private isRefreshing = false; private pendingRefreshTrigger: Trigger | null = null; private isDisposed = false; private pollHandle: ReturnType<PollingPolicy["start"]> | undefined; private hasStartupTickRun = false; private currentRefresh: Promise<void> | null = null;
  private hasAuthenticatedNetworkBootstrap = false; private hasLiveNetworkBootstrap = false; private authRevision = 0; private snapshot: Snapshot;
  private readonly listeners = new Set<(snapshot: Snapshot) => void>(); private readonly gateProperties = new Map<FeatureFlagName, MutableGateProperty>(); private readonly overrideStore: SandFeatureFlagOverrideStore; private isAnysphereUser = false;
  private readonly loggedModelConfigRejections = new Map<string, Set<ModelConfigRejection>>(); private rateLimitedUntilMs: number | undefined; private flagsFetchedAtMs: number | undefined;
  private readonly bootstrapDeadline; private readonly refreshPoll; private lastHydratedUserId: string | null = null; private hydratedUserIdReady: Promise<boolean> | null = null; private resolveHydratedUserIdReady: ((value: boolean) => void) | null = null;
  constructor(private readonly options: { getAccessToken(options: { backendUrl: string }): Promise<string>; getMachineId(): Promise<string>; getCacheDir(): string; isDevBuild?: boolean; bootstrapTimeoutMs?: number; pollIntervalMs?: number; env?: NodeJS.ProcessEnv }) {
    this.bootstrapDeadline = createDeadlinePolicy(realClock, { name: "sand-experiments-bootstrap", timeoutMs: options.bootstrapTimeoutMs ?? BOOTSTRAP_TIMEOUT_MS });
    this.refreshPoll = createPollingPolicy(realClock, { name: "sand-experiments-refresh-poll", intervalMs: options.pollIntervalMs ?? jitteredSandExperimentPollIntervalMs(options.isDevBuild === true) });
    this.overrideStore = new SandFeatureFlagOverrideStore(options.getCacheDir); if (this.canUseFeatureFlagOverrides()) this.overrideStore.hydrateFromDisk(); this.snapshot = this.computeSnapshot();
  }
  start(): void { const cached = loadCachedBootstrap(this.options.getCacheDir()); if (cached != null) { try { this.hydrate(cached.config); this.flagsFetchedAtMs = cached.fetchedAtMs; } catch (error) { reportExperimentsDiagnostic({ kind: "bootstrap_cache_hydrate_failed", errorClass: errorLogTag(error) }); } } this.refreshSnapshot(); this.pollHandle = this.refreshPoll.start(() => { const trigger = this.hasStartupTickRun ? "poll" : "startup"; this.hasStartupTickRun = true; return this.refresh(trigger); }); }
  handleAuthChange(): void { this.authRevision += 1; void this.refresh("auth_change"); }
  async refreshNow(): Promise<void> { await this.refresh("manual"); }
  subscribe(listener: (snapshot: Snapshot) => void): () => void { this.listeners.add(listener); return () => this.listeners.delete(listener); }
  getSnapshot(): Snapshot { return this.snapshot; }
  getFeatureGateProperty(name: FeatureFlagName): MutableGateProperty { let property = this.gateProperties.get(name); if (property == null) { property = new MutableGateProperty(this.checkFeatureGate(name)); this.gateProperties.set(name, property); } return property; }
  pinGateOnAuthenticatedBootstrap(name: FeatureFlagName, pin: (value: boolean) => void): void { if (this.hasAuthenticatedNetworkBootstrap) { pin(this.checkFeatureGate(name)); return; } const unsubscribe = this.subscribe(() => { if (!this.hasAuthenticatedNetworkBootstrap) return; unsubscribe(); pin(this.checkFeatureGate(name)); }); }
  checkFeatureGate(name: FeatureFlagName): boolean { const local = this.overrideStore.read(name); if (local != null) return local; const environment = this.canUseFeatureFlagOverrides() ? envGateOverride(name, this.options.env) : undefined; if (environment != null) return environment; if (this.client == null) return FLAGS[name]?.default ?? false; try { return this.client.checkGate(name, READ_OPTIONS); } catch { return FLAGS[name]?.default ?? false; } }
  hasAuthenticatedStatsigBootstrap(): boolean { return this.hasAuthenticatedNetworkBootstrap; }
  hasLiveStatsigBootstrap(): boolean { return this.hasLiveNetworkBootstrap; }
  getFlagsAgeMs(): number | undefined { return this.flagsFetchedAtMs == null ? undefined : Math.max(0, Date.now() - this.flagsFetchedAtMs); }
  canUseFeatureFlagOverrides(): boolean { return this.options.isDevBuild === true || this.isAnysphereUser; }
  setIsAnysphereUser(value: boolean): void { if (this.isAnysphereUser === value) return; const before = this.canUseFeatureFlagOverrides(); this.isAnysphereUser = value; const after = this.canUseFeatureFlagOverrides(); if (before === after) return; if (after) this.overrideStore.hydrateFromDisk(); else this.overrideStore.clearAll(); this.refreshSnapshot(); }
  getFeatureFlagOverrides(): Map<FeatureFlagName, boolean> { return this.overrideStore.activeOverrides(); }
  setFeatureFlagOverride(name: string, value: boolean): void { if (this.canUseFeatureFlagOverrides() && this.overrideStore.set(name, value)) this.persistAndBroadcastOverrides(); }
  clearFeatureFlagOverride(name: FeatureFlagName): void { if (this.canUseFeatureFlagOverrides() && this.overrideStore.clear(name)) this.persistAndBroadcastOverrides(); }
  clearAllFeatureFlagOverrides(): void { if (!this.canUseFeatureFlagOverrides() || this.overrideStore.size === 0) return; this.overrideStore.clearAll(); this.persistAndBroadcastOverrides(); }
  setAllFeatureFlagOverridesToBundledValues(): void { if (!this.canUseFeatureFlagOverrides()) return; this.overrideStore.setAllToBundledDefaults(); this.persistAndBroadcastOverrides(); }
  replaceFeatureFlagOverrides(overrides: Record<string, boolean>): void { this.overrideStore.replaceAll(overrides); this.persistAndBroadcastOverrides(); }
  getFeatureFlagOverridesRecord(): Partial<Record<FeatureFlagName, boolean>> { return this.overrideStore.activeRecord(); }
  applyFeatureFlagOverrideCommand(command: { kind: "set"; name: string; value: boolean } | { kind: "clear"; name: string } | { kind: "clear-all" } | { kind: "set-all-bundled" }): void { if (command.kind === "set" && isFlagName(command.name)) this.setFeatureFlagOverride(command.name, command.value); else if (command.kind === "clear" && isFlagName(command.name)) this.clearFeatureFlagOverride(command.name); else if (command.kind === "clear-all") this.clearAllFeatureFlagOverrides(); else if (command.kind === "set-all-bundled") this.setAllFeatureFlagOverridesToBundledValues(); }
  private persistAndBroadcastOverrides(): void { void this.overrideStore.persist(); this.refreshSnapshot(); }
  private computeFeatureFlagDevPanel(): unknown { if (!this.canUseFeatureFlagOverrides()) return undefined; const overrides = this.overrideStore.activeOverrides(); return { items: Object.keys(FLAGS).filter(isFlagName).map((name) => ({ name, value: this.checkFeatureGate(name), defaultValue: FLAGS[name]?.default ?? false, override: overrides.get(name) ?? null })), isLive: this.hasLiveNetworkBootstrap }; }
  private async whenReady(timeoutMs = GATE_READY_TIMEOUT_MS): Promise<void> { if (this.hasLiveNetworkBootstrap) return; const drain = async () => { let last: Promise<void> | null = null; while (!this.hasLiveNetworkBootstrap && !this.isDisposed) { const current = this.currentRefresh; if (current == null || current === last) return; last = current; await current; } }; if (timeoutMs <= 0) return drain(); const deadline = createDeadlinePolicy(realClock, { name: "sand-experiments-gate-ready", timeoutMs }); try { await deadline.run(drain); } catch (error) { if (!(error instanceof DeadlineExceededError)) throw error; } }
  async checkGate(name: FeatureFlagName, options?: { timeoutMs?: number }): Promise<boolean> { await this.whenReady(options?.timeoutMs); return this.checkFeatureGate(name); }
  getExperiment(name: ExperimentName): Record<string, unknown> { const fallback = EXPERIMENTS[name].fallbackValues as Record<string, unknown>; if (this.client == null) return fallback; try { return { ...fallback, ...this.client.getExperiment(name, READ_OPTIONS).value }; } catch { return fallback; } }
  getExperimentGroupName(name: ExperimentName): string | null { if (this.client == null) return null; try { return this.client.getExperiment(name, READ_OPTIONS).groupName ?? null; } catch { return null; } }
  hasHydratedStatsigUserId(): boolean { return this.lastHydratedUserId != null && this.lastHydratedUserId.length > 0; }
  async waitForHydratedStatsigUserId(timeoutMs = 60_000): Promise<boolean> { if (this.hasHydratedStatsigUserId()) return true; if (this.hydratedUserIdReady == null) this.hydratedUserIdReady = new Promise((resolve) => { this.resolveHydratedUserIdReady = resolve; }); const deadline = createDeadlinePolicy(realClock, { name: "sand-experiments-hydrated-user", timeoutMs }); try { return await deadline.run(() => this.hydratedUserIdReady as Promise<boolean>); } catch (error) { if (error instanceof DeadlineExceededError) return false; throw error; } }
  getSandModelExperimentState(): unknown { const environment = readSandModelExperimentEnvOverride(this.options.env); if (environment != null) return environment; if (!this.hasHydratedStatsigUserId() || !Object.hasOwn(EXPERIMENTS, SAND_MODEL_EXPERIMENT_NAME)) return undefined; const name = SAND_MODEL_EXPERIMENT_NAME as ExperimentName; return resolveSandModelExperimentState({ groupName: this.getExperimentGroupName(name), enabled: this.getExperiment(name).enabled === true }); }
  logSandModelExperimentExposure(): boolean { if (this.client == null || !this.hasHydratedStatsigUserId() || !Object.hasOwn(EXPERIMENTS, SAND_MODEL_EXPERIMENT_NAME)) return false; try { const value = this.client.getExperiment(SAND_MODEL_EXPERIMENT_NAME, READ_OPTIONS); if (value.groupName == null) return false; this.client.getExperiment(SAND_MODEL_EXPERIMENT_NAME); void this.client.flush().catch((error) => reportExperimentsDiagnostic({ kind: "exposure_flush_failed", errorClass: errorLogTag(error) })); return true; } catch { return false; } }
  getDynamicConfig(name: DynamicConfigName): Record<string, unknown> { const fallback = DYNAMIC_CONFIGS[name].fallbackValues as Record<string, unknown>; if (this.client == null) return fallback; try { return { ...fallback, ...this.client.getDynamicConfig(name, READ_OPTIONS).value }; } catch { return fallback; } }
  getConfiguredDefaultModel() { return this.resolveConfiguredModel(SAND_DEFAULT_MODEL_CONFIG_NAME); }
  getConfiguredAutomationsModel() { return this.resolveConfiguredModel(SAND_AUTOMATIONS_MODEL_CONFIG_NAME); }
  private resolveConfiguredModel(configName: string) { const resolution = resolveSandDefaultModelConfig({ raw: Object.hasOwn(DYNAMIC_CONFIGS, configName) ? this.getDynamicConfig(configName as DynamicConfigName) : undefined, hasHydratedStatsigUserId: this.hasHydratedStatsigUserId() }); if (resolution.rejection != null) this.reportModelConfigRejection(configName, resolution.rejection); return resolution.selection; }
  private reportModelConfigRejection(configName: string, rejection: ModelConfigRejection): void { const logged = this.loggedModelConfigRejections.get(configName) ?? new Set<ModelConfigRejection>(); this.loggedModelConfigRejections.set(configName, logged); if (logged.has(rejection)) return; logged.add(rejection); reportExperimentsDiagnostic({ kind: "config_not_applied", stage: configName, reason: rejection }); }
  getComputerUseModelOverride(): Record<string, unknown> | undefined { return this.checkFeatureGate("sand_computer_use_playwright") ? this.getDynamicConfig("sand_computer_use_playwright_config") : undefined; }
  getBrowserUseModelOverride(): Record<string, unknown> | undefined { return this.checkFeatureGate("sand_browser_use_subagent") ? this.getDynamicConfig("sand_browser_use_model") : undefined; }
  async dispose(): Promise<void> { this.isDisposed = true; this.pollHandle?.dispose(); this.pollHandle = undefined; this.resolveHydratedUserIdReady?.(false); this.resolveHydratedUserIdReady = null; this.hydratedUserIdReady = null; this.hasAuthenticatedNetworkBootstrap = false; this.listeners.clear(); for (const property of this.gateProperties.values()) property.clearListeners(); const client = this.client; this.client = null; try { await client?.shutdown(); } catch (error) { reportExperimentsDiagnostic({ kind: "shutdown_failed", errorClass: errorLogTag(error) }); } }
  private refresh(trigger: Trigger): Promise<void> { if (this.isDisposed) return Promise.resolve(); if (this.isRefreshing) { this.pendingRefreshTrigger = trigger; return this.currentRefresh ?? Promise.resolve(); } this.isRefreshing = true; const work = this.runRefresh(trigger); this.currentRefresh = work; return work; }
  private async waitOutRateLimit(): Promise<void> { if (this.rateLimitedUntilMs == null) return; const remaining = Math.max(0, this.rateLimitedUntilMs - realClock.now()); if (remaining <= 0) return; await new Promise<void>((resolve) => { realClock.schedule(remaining, resolve); }); }
  private async runRefresh(trigger: Trigger): Promise<void> { try { if (trigger === "auth_change") this.rateLimitedUntilMs = undefined; else { await this.waitOutRateLimit(); if (this.isDisposed) return; } const revision = this.authRevision; const result = await fetchStatsigBootstrap({ backendUrl: getConfiguredBackendUrl(this.options.env), deadline: this.bootstrapDeadline, getAccessToken: this.options.getAccessToken, getMachineId: this.options.getMachineId, env: this.options.env }); if (this.isDisposed) return; if (result.config == null) { if (result.retryAfterMs != null && result.retryAfterMs > 0) this.rateLimitedUntilMs = realClock.now() + result.retryAfterMs; return; } this.rateLimitedUntilMs = undefined; if (revision !== this.authRevision) { reportExperimentsDiagnostic({ kind: "bootstrap_discarded_auth_changed", stage: trigger }); return; } const userId = readStatsigBootstrapUserId(result.config); this.hydrate(result.config); this.flagsFetchedAtMs = Date.now(); this.hasLiveNetworkBootstrap = true; this.hasAuthenticatedNetworkBootstrap = userId != null && userId.length > 0; await saveCachedBootstrap(this.options.getCacheDir(), { config: result.config, userId, fetchedAtMs: this.flagsFetchedAtMs }); this.refreshSnapshot(); reportExperimentsDiagnostic({ kind: "bootstrap_resolved", stage: trigger, authenticated: this.hasAuthenticatedNetworkBootstrap, gatesOnCount: Object.values(this.snapshot.featureGates).filter(Boolean).length }); } catch (error) { reportExperimentsDiagnostic({ kind: "bootstrap_failed", stage: trigger, errorClass: errorLogTag(error) }); } finally { this.isRefreshing = false; const pending = this.pendingRefreshTrigger; this.pendingRefreshTrigger = null; if (pending != null && !this.isDisposed) void this.refresh(pending); } }
  private settleHydratedUserIdReady(): void { if (!this.hasHydratedStatsigUserId()) return; this.resolveHydratedUserIdReady?.(true); this.resolveHydratedUserIdReady = null; this.hydratedUserIdReady = null; }
  private hydrate(config: string): void { const user = extractStatsigUser(config) as StatsigUser; this.lastHydratedUserId = typeof user.userID === "string" ? user.userID : null; if (this.client == null) { this.client = new StatsigClient(STATSIG_CLIENT_KEY, user, { loggingEnabled: "always", disableStorage: true, logEventCompressionMode: LogEventCompressionMode.Forced, networkConfig: { api: STATSIG_LOG_EVENT_PROXY_URL, networkOverrideFunc: sandStatsigNetworkOverride } }); this.client.dataAdapter.setData(config); this.client.initializeSync(); } else { this.client.dataAdapter.setData(config); this.client.updateUserSync(user); } this.isInitialized = true; this.settleHydratedUserIdReady(); }
  private computeSnapshot(): Snapshot { const featureGates: Record<string, boolean> = {}; for (const name of Object.keys(FLAGS)) if (isFlagName(name)) featureGates[name] = this.checkFeatureGate(name); const experiments: Record<string, Record<string, unknown>> = {}; for (const name of Object.keys(EXPERIMENTS) as ExperimentName[]) experiments[name] = this.getExperiment(name); const dynamicConfigs: Record<string, Record<string, unknown>> = {}; for (const name of Object.keys(DYNAMIC_CONFIGS) as DynamicConfigName[]) dynamicConfigs[name] = this.getDynamicConfig(name); const sandModelExperiment = this.getSandModelExperimentState(); const raw = this.hasAuthenticatedNetworkBootstrap && sandModelExperiment == null && Object.hasOwn(DYNAMIC_CONFIGS, SAND_MODEL_FILTER_CONFIG_NAME) ? this.getDynamicConfig(SAND_MODEL_FILTER_CONFIG_NAME as DynamicConfigName).allowedModelIds : undefined; const allowed = Array.isArray(raw) && raw.length > 0 && raw.every((id) => typeof id === "string") ? [...new Set(raw as string[])] : undefined; return { isInitialized: this.isInitialized, featureGates, experiments, dynamicConfigs, ...(sandModelExperiment === undefined ? {} : { sandModelExperiment }), ...(allowed === undefined ? {} : { sandModelFilterAllowedIds: allowed }), ...(this.computeFeatureFlagDevPanel() === undefined ? {} : { featureFlags: this.computeFeatureFlagDevPanel() }) }; }
  private refreshSnapshot(): void { this.snapshot = this.computeSnapshot(); for (const [name, property] of this.gateProperties) property.set(this.snapshot.featureGates[name] ?? false); for (const listener of this.listeners) { try { listener(this.snapshot); } catch (error) { reportExperimentsDiagnostic({ kind: "snapshot_listener_failed", errorClass: errorLogTag(error) }); } } }
}
