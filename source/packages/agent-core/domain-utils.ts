// These retained module-initializer values are deliberately private. Their
// original public/type surface did not survive tree shaking.
const BIGINT_ZERO = BigInt(0);
const BIGINT_EIGHT = BigInt(8);
const BIGINT_SIXTEEN = BigInt(16);
const IPV4_LOW_HEXTET_MASK = BigInt(65_535);

void BIGINT_ZERO;
void BIGINT_EIGHT;
void BIGINT_SIXTEEN;
void IPV4_LOW_HEXTET_MASK;
