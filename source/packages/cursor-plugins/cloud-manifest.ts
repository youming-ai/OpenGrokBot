export function getReleasePluginSource(fields: { releaseTag?: string; releaseRepo?: string; releaseAsset?: string }, fallbackReleaseTag?: string): { kind: "release"; releaseRepo: string; releaseAsset: string; releaseTag: string } | undefined { const releaseTag = fields.releaseTag ?? fallbackReleaseTag; return fields.releaseRepo && fields.releaseAsset && releaseTag ? { kind: "release", releaseRepo: fields.releaseRepo, releaseAsset: fields.releaseAsset, releaseTag } : undefined; }
