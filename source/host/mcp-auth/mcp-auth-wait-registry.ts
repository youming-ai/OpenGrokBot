export const DEFAULT_MCP_AUTH_WAIT_TTL_MS = 60 * 60 * 1_000;
export function normalizeConnectorName(value: string): string { return value.toLowerCase().replace(/[^a-z0-9]/g, ""); }
export interface McpAuthCompletionIdentity { readonly serverId: string; readonly serverName: string }
export class McpAuthWaitRegistry { readonly #waits = new Map<string, { agentId: string; serverId: string | null; expiresAtMs: number }>(); readonly #ttlMs: number; readonly #now: () => number; constructor(options?: { readonly ttlMs?: number; readonly now?: () => number }) { this.#ttlMs = options?.ttlMs ?? DEFAULT_MCP_AUTH_WAIT_TTL_MS; this.#now = options?.now ?? Date.now; }
  register(event: { readonly agentId: string; readonly connector: string; readonly serverId?: string | null }): void { this.prune(); const serverId = event.serverId != null && event.serverId.length > 0 ? event.serverId : null; const nameKey = normalizeConnectorName(event.connector); const key = nameKey.length > 0 ? nameKey : serverId != null ? `id:${serverId}` : null; if (key != null) this.#waits.set(key, { agentId: event.agentId, serverId, expiresAtMs: this.#now() + this.#ttlMs }); }
  take(completion: McpAuthCompletionIdentity): string | null { this.prune(); const nameKey = normalizeConnectorName(completion.serverName); let byServerId: { agentId: string } | null = null; let byName: { agentId: string } | null = null; for (const [key, entry] of [...this.#waits]) { const idMatch = entry.serverId != null && entry.serverId === completion.serverId; const nameMatch = entry.serverId == null && nameKey.length > 0 && key === nameKey; if (!idMatch && !nameMatch) continue; this.#waits.delete(key); if (idMatch) byServerId = entry; else byName = entry; } return (byServerId ?? byName)?.agentId ?? null; }
  prune(): void { const now = this.#now(); for (const [key, entry] of [...this.#waits]) if (entry.expiresAtMs <= now) this.#waits.delete(key); }
}
