import { join } from "node:path"; export const ATTACHMENTS_DIRNAME = "attachments"; export const ASSETS_DIRNAME = "assets"; export function getAgentAttachmentsDir(agentDir: string): string { return join(agentDir, ATTACHMENTS_DIRNAME); } export function getAgentAssetsDir(agentDir: string): string { return join(agentDir, ASSETS_DIRNAME); } export function getAgentMediaStoreRoots(agentDir: string): [string, string] { return [getAgentAttachmentsDir(agentDir), getAgentAssetsDir(agentDir)]; }
