import { execFile } from "node:child_process"; import { existsSync, lstatSync } from "node:fs"; import { readdir } from "node:fs/promises"; import { join } from "node:path";
export const SAND_WALLPAPER_COMMAND = "/usr/local/bin/sand-wallpaper"; export const SAND_WALLPAPER_TONE_SCRIPT = "/usr/local/bin/sand-wallpaper-tone.mjs"; export const X_SOCKET_DIR = "/tmp/.X11-unix"; export const COMMAND_TIMEOUT_MS = 10_000; const X_SOCKET_NAME = /^X(\d+)$/;
export function parseTonePlan(stdout: string): { tone: string; msUntilNextBoundary: number } | undefined { const [tone, seconds, ...rest] = stdout.trim().split(/\s+/); if (tone === undefined || seconds === undefined || rest.length > 0 || !/^[a-z]+$/.test(tone) || !/^\d+$/.test(seconds)) return undefined; return { tone, msUntilNextBoundary: Number(seconds) * 1_000 }; }
export async function readLiveDisplays(socketDir: string): Promise<Array<{ display: string; uid: number; gid: number }>> { const entries = await readdir(socketDir).catch(() => []); const numbers = entries.map((entry) => X_SOCKET_NAME.exec(entry)?.[1]).filter((num): num is string => num !== undefined).sort((a, b) => Number(a) - Number(b)); const displays = []; for (const num of numbers) { const stats = lstatSync(join(socketDir, `X${num}`), { throwIfNoEntry: false }); if (stats?.isSocket() !== true) continue; displays.push({ display: `:${num}`, uid: stats.uid, gid: stats.gid }); } return displays; }
type RunCommand = (file: string, args: readonly string[], options: { timeout: number; uid?: number; gid?: number }) => Promise<{ stdout: string }>;
function defaultRunCommand(file: string, args: readonly string[], options: { timeout: number; uid?: number; gid?: number }): Promise<{ stdout: string }> { return new Promise((resolve, reject) => execFile(file, args, options, (error, stdout) => error == null ? resolve({ stdout }) : reject(error))); }
export function asDisplayOwner(owner: { uid: number; gid: number }, getuid = process.getuid?.bind(process), getgid = process.getgid?.bind(process)): {} | { uid: number; gid: number } { const self = getuid?.(); return self !== 0 || (owner.uid === self && owner.gid === getgid?.()) ? {} : owner; }
export function createBoxWallpaperCommands(options: { readonly settingsPath: string; readonly log: (message: string) => void; readonly nodePath?: string; readonly toneScriptPath?: string; readonly wallpaperCommandPath?: string; readonly xSocketDir?: string; readonly runCommand?: RunCommand; readonly readDisplays?: (directory: string) => Promise<Array<{ display: string; uid: number; gid: number }>>; readonly exists?: (path: string) => boolean }) { const nodePath = options.nodePath ?? process.execPath; const toneScriptPath = options.toneScriptPath ?? SAND_WALLPAPER_TONE_SCRIPT; const wallpaperCommandPath = options.wallpaperCommandPath ?? SAND_WALLPAPER_COMMAND; const socketDir = options.xSocketDir ?? X_SOCKET_DIR; const run = options.runCommand ?? defaultRunCommand; const exists = options.exists ?? existsSync; return { isAvailable: exists(toneScriptPath) && exists(wallpaperCommandPath), async resolvePlan() { const result = await run(nodePath, [toneScriptPath, options.settingsPath], { timeout: COMMAND_TIMEOUT_MS }).catch((error) => { options.log(`wallpaper: tone helper failed (${error instanceof Error ? error.message : String(error)})`); return undefined; }); if (result === undefined) return undefined; const plan = parseTonePlan(result.stdout); if (plan === undefined) options.log("wallpaper: tone helper printed an unusable plan"); return plan; }, async paint() { try { for (const owner of await (options.readDisplays ?? readLiveDisplays)(socketDir)) await run(wallpaperCommandPath, ["paint", owner.display], { timeout: COMMAND_TIMEOUT_MS, ...asDisplayOwner(owner) }).catch((error) => options.log(`wallpaper: paint of ${owner.display} failed (${error instanceof Error ? error.message : String(error)})`)); } catch (error) { options.log(`wallpaper: reading live displays failed (${error instanceof Error ? error.message : String(error)})`); } } }; }
